<%= form_for deployment_window_series_occurrence_path do |f| %>
  <% if @occurrence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@occurrence.errors.count, "error") %> prohibited this occurrence from being saved:</h2>

      <ul>
      <% @occurrence.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :start_at %>
    <%= f.time_select :start_at, :class => 'initialFocus' %>
  </div>

  <div class="field">
    <%= f.label :finish_at %>
    <%= f.time_select :finish_at %>
    <br />
  </div>

  <div class="actions">
    <%= f.submit "Save", :class => "button" %>
    <br />
  </div>
<br />
<% end %>
